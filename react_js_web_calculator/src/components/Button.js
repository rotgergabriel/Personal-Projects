import React from "react";

const Button = (props) => {
    return(
        <>
            <button value={props}>
                Nan
            </button>
        </>
    )
}

export default Button;